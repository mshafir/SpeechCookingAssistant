#JSGF V1.0;

/**
 * JSGF Grammar for Hello World example
 */

grammar speech;

<start> = sue chef ;

<stepQuestion> = <start> ((tell me what to do) | (what do I do) | (what happens));

<ingredientQuestion> = <start> how (much | many);